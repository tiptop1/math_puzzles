import 'package:flutter/material.dart';

extension CustomColorScheme on ColorScheme {
  Color get correctAnswer => Colors.green;

  Color get incorrectAnswer => Colors.red;

  Color get answer => Colors.blue;
}
